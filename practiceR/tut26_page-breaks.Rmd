---
title: "Inserting a page break"
author: "Richard Layton"
date: "October 17, 2017"
output:
  word_document:
    reference_docx: "../common/word-styles-reference-05.docx"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
library(docxtools)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

###### page break
## Including Plots

You can also embed plots, for example:

```{r fig.height = 1}
put_gap()
```

```{r pressure, echo=FALSE}
plot(pressure)
```

###### page break



```{r fig.height = 2}
put_gap()
```


Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.




```{r}
set.seed(20170913)
n <- 5
a <- sample(letters, n)
b <- sample(letters, n)
w <- runif(n, min = -5, max = 50) * 1e+5
y <- runif(n, min = -25, max = 40) / 1e+3
z <- runif(n, min = -5, max = 100)
x <- data.frame(w, a, y, b, z, stringsAsFactors = FALSE)

output <- format_engr(x, sigdig = c(3, 4, 5))

```


```{r}
align_pander(output, align_idx = "rcrcr")
```


###### page break

#####`r knitr::include_graphics('../visuals/1Dbar_no3.png')`






